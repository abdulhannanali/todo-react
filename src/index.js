var React = require('react')
var ReactDOM = require('react-dom')

var TodoListComponent = require("./components/TodoListComponent.js")

ReactDOM.render(
  <TodoListComponent />,
  document.getElementById("divContainer")
)
  
